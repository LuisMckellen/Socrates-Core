Author diagnostic_goal for the two s_cell_theory_L1 misconceptions

A/B/C hint test on Local CPU (6 inputs x 3 reps): with the goal line,
ct_spontaneous_generation hints went from 0/3 model hints (rule 3c
fallback every rep) to 3/3 hints aimed at cell origin, and
ct_hypertrophy hints narrowed from "size and number" to "number".
Fallback rate 6/18 -> 3/18 (the remaining 3 are the partial control).

Both goals are worded to avoid key-term forms ("existing") that rule
3c rejects on the targeted path. Classifier prompt unaffected.
